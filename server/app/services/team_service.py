from django.shortcuts import get_object_or_404\nfrom .models import LeaveRequest, Attendance\n\n\ndef get_team_attendance(manager_id):\n    # Assuming you have a method to get employee ids under a manager\n    employee_ids = get_employee_ids_by_manager(manager_id)\n    attendance_records = Attendance.objects.filter(employee__employee_id__in=employee_ids)\n    return attendance_records\n\ndef get_team_leave_requests(manager_id):\n    employee_ids = get_employee_ids_by_manager(manager_id)\n    leave_requests = LeaveRequest.objects.filter(employee__employee_id__in=employee_ids)\n    return leave_requests\n\ndef approve_leave_request(manager_id, request_id):\n    leave_request = get_object_or_404(LeaveRequest, id=request_id)\n    if leave_request.status == 'Pending':\n        leave_request.status = 'Approved'\n        leave_request.save()\n        return {'status': 'Leave request approved'}\n    return {'status': 'Leave request cannot be approved'}, 400\n\ndef deny_leave_request(manager_id, request_id):\n    leave_request = get_object_or_404(LeaveRequest, id=request_id)\n    if leave_request.status == 'Pending':\n        leave_request.status = 'Denied'\n        leave_request.save()\n        return {'status': 'Leave request denied'}\n    return {'status': 'Leave request cannot be denied'}, 400